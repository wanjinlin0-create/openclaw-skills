# 多智能体可视化平台 - 后端

基于 FastAPI + WebSocket 实现

## 安装依赖

```bash
pip install fastapi uvicorn websockets pydantic
```

## 运行

```bash
uvicorn main:app --host 0.0.0.0 --port 8080
```

然后打开浏览器访问: http://localhost:8080
